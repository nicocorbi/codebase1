puedes buscar archivos sin tener que gestionar un servidor 
SQL-> CREATE -> data.db
find/-name *.sqlite
SQL-lo bueno es que solo lo aprendes una vez

-ejecutar .sql
-ejecutar query en CLI
users.db < create.sql rediriges datos
una clave primaria es la forma de identificar cada fila que tenemos en la tabla
y nos va a permitir relacionar datos en la tabla.

DELETE from users WHERE username = "admin";
UPDATE actualizar
UPDATE users SET password = "adminpasspass" WHERE username = "admin";
